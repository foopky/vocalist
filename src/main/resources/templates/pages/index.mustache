<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>단어장 템플릿</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f5f5f5;
        }
        h1 {
            color: #333;
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #fff;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .word-row {
            cursor: pointer;
        }
        .word-row:hover {
            background-color: #f9f9f9;
        }
        #add-word-form {
            margin-top: 20px;
            text-align: center;
        }
        button {
            padding: 8px 16px;
            margin: 5px;
            border: none;
            border-radius: 4px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
            font-size: 14px;
        }
        button:hover {
            background-color: #45a049;
        }
        .sort-buttons div {
            margin-bottom: 10px;
        }
        .sort-buttons button {
            background-color: #2196F3;
        }
        .sort-buttons button:hover {
            background-color: #0b7dda;
        }
        a {
            display: inline-block;
            margin: 20px 0;
            padding: 10px 20px;
            background-color: #2196F3;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }
        a:hover {
            background-color: #0b7dda;
        }
        .increase-decrease-buttons form {
            display: inline;
        }
        .increase-decrease-buttons button {
            background-color: #FF9800;
        }
        .increase-decrease-buttons button:hover {
            background-color: #e68a00;
        }
    </style>
</head>
<body>

<h1>단어장</h1>
<div class="sort-buttons">
    <form action="/voca/sort" method="get">
        <button value="0" type="submit" name="condition">기본정렬</button>
        <div>오름차순:
            <button value="5" type="submit" name="condition">ID</button>
            <button value="6" type="submit" name="condition">단어</button>
            <button value="7" type="submit" name="condition">중요도</button>
            <button value="8" type="submit" name="condition">카운트</button>
        </div>
        <div>내림차순:
            <button value="1" type="submit" name="condition">ID</button>
            <button value="2" type="submit" name="condition">단어</button>
            <button value="3" type="submit" name="condition">중요도</button>
            <button value="4" type="submit" name="condition">카운트</button>
        </div>
    </form>
</div>
<table>
    <thead>
    <tr>
        <th>단어명</th>
        <th>뜻</th>
        <th>중요도</th>
        <th>카운트</th>
    </tr>
    </thead>
    <tbody>
    {{#vocabularies}}
        <tr class="word-row">
            <td>{{word}} <a href="/voca/{{id}}/edit">수정하기</a></td>
            <td>{{meaning}}</td>
            <td>{{importance}}</td>
            <td class="increase-decrease-buttons">{{count}}
                <form action="/voca/{{id}}/increase" method="get"><button>증가</button></form>
                <form action="/voca/{{id}}/decrease" method="get"><button>감소</button></form>
                <form action="/Bookmarked/{{id}}" method="post"><button>북마크 추가하기</button></form>
            </td>
        </tr>
    {{/vocabularies}}
    </tbody>
</table>

<div id="add-word-form">
    <a href="/voca/new">단어 추가하기</a>
    <a href="/Bookmarks">북마크</a>
</div>

</body>
</html>
